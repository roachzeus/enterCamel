apiVersion: v1
kind: Service
metadata:
  name: enter-camel-service
spec:
  selector:
    app: enter-camel
  type: NodePort
  #type: LoadBalancer
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080